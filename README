Configuration
-------------
Copy 'config.py.ex' to 'config.py' and adjust the 'dbengine' variable to your local configuration.

Place the contents of client/html/ in www-root make sure you also copy .htaccess
Adjust the "impressum.html.ex" and save as "impressum.html"
Place *.py and .htaccess in www-root/api
Install neccessary python libraries:
pip3 install --user request pytz sqlalchemy flipflop mysqlclient

Initialize database:
python3 init.py

